CREATE OR REPLACE TABLE `main`.`gold`.`stg_sales_part3`
( 
	`ProductID` INT,
	`OrderYear` INT,
	`OrderMonth` INT,
	`TotalQty` INT,
	`TotalRevenue` DOUBLE,
	`RunningRevenue` DOUBLE,
	`RevenueRank` BIGINT
);
